/**
 * 学习画像域 —— 已迁移至 ./diagnosis。
 *
 * 原先的 LearnerProfile / MasteryRecord 为早期占位模型，已由
 * `./diagnosis`（LearnerProfile —— Derived Read Model）取代。
 * 当前掌握状态的事实来源为后端 MasteryRecord（apps/api），领域名词保持一致。
 */
export {};
