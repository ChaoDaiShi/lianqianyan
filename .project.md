# 项目记忆：忆涟千言—教（EducationMind）

## 项目概述

- **中文名称**：忆涟千言—教
- **英文领域名称**：EducationMind
- **定位**：基于学习画像、学习证据与个性化学习规划的 AI 智能学习伙伴
- **模版类型**：`react-vite`（React 18 + TypeScript + Vite + Tailwind CSS + React Router hash 模式）
- **AI 助手名**：小涟（Education Agent）

## 当前开发阶段：Phase 3-6 Live2D Digital Human & Learning Workshop

- **本地 Live2D**：使用用户提供的 Cyrene1002 模型替换旧静态人物；模型、纹理、`.moc3` 与 Cubism Core 只安装到被忽略的 `.local/live2d/`，开发服务器专用挂载，Git 与 `dist/` 不携带受限版权资产。
- **数字人讲解**：小涟页与学习空间的真实 Tutor 回答提供浏览器 TTS 播放/停止，播放状态驱动 Live2D 口型；不宣称语音识别或服务端语音合成。
- **学习工坊**：`/#/resources` 提供课程模板资源生成、固定 Wikipedia 来源的联网检索、受限 `c-edu` 五阶段编译模拟；三个服务均有正式 API、类型客户端、错误态与来源/安全边界。
- **GPU 稳定性**：全局 Live2D 租约保证同一页面至多一个 Pixi/WebGL 模型实例，备用位置显示代码原生“涟”徽记；注册 Pixi Ticker，避免跨上下文纹理与模型不更新问题。
- **浏览器验证**：桌面与 390px 移动端均验证 Live2D 实际画布、资源 Markdown 下载、Wikipedia 真实结果、编译 stdout、Tutor 请求与播放/停止口型联动；控制台错误/警告、页面异常、失败请求和 HTTP 4xx/5xx 均为 0。

## 历史阶段：Phase 3-5 Education Tools & MCP

- **共享能力边界**：七个 Education Tools 位于 `apps/api/app/tools/`，内部 Agent 与外部 MCP Client 都经 `EducationToolRegistry` 直接调用既有 Application Service；Tool 和 MCP 不重新实现业务逻辑，不通过 HTTP 调自己 API。
- **Tool Catalog**：`GET /api/tools` 返回真实定义与同源 Pydantic input schema；`generate_study_plan`、`replan_study_plan` 是唯一写工具，`evaluate_practice` 不暴露。
- **Agent Trace**：Diagnosis/Planner/Assessment/Knowledge 通过共享 Registry 实际执行后追加真实 Tool 节点；Web 使用不同大小的 Agent 卡和 Tool 胶囊。
- **MCP Server**：`mcp/server/server.py` 使用官方 Python MCP SDK stdio transport，支持 initialize、tools/list、tools/call、structuredContent、30 秒超时和安全错误；stdout 只输出协议，日志走 stderr。`mcp/server/smoke.py` 为真实子进程协议验证。
- **Skill 文档**：`docs/skills/` 仅包含 learner-diagnosis、adaptive-planning、grounded-tutoring、learning-assessment 四份描述性 workflow，不加入 Skill Runtime。
- **Tests / gates**：新增 Tool Registry、Adapter、Catalog、Agent Trace、MCP protocol tests；提交前运行后端全量 pytest、真实 MCP smoke、`pnpm check`、`pnpm build`、污染扫描与 `git diff --check`。

- **Current Plan lifecycle**：业务层保证同一 learner/course 至多一个 ACTIVE；读取遗留重复 ACTIVE 时返回 generated_at 最新的一条并记录 warning，不在 GET 中修库。
- **Dynamic Replanning**：Practice 提交 Evidence + Mastery 后，在独立事务中复用 Diagnosis → Planner，并由确定性的 ReplanningPolicy 比较有序 `(knowledge_point_id, action_type)` 签名；只有 Material Change 才原子替换旧计划。
- **Replanning API**：`POST /api/plans/replan` 返回 `status=not_needed|performed|failed`、reason codes、旧/新计划与 top-task preview；无 Current Plan 返回 `NO_ACTIVE_PLAN` 且不自动创建计划。
- **Practice contract**：Evidence + Mastery 先提交；重规划失败不回滚练习事实，响应明确区分 `replanning.status=failed`。
- **Web**：LearningSpace 练习后刷新 Profile、Diagnosis、Current Plan，并显示计划已调整、无需调整或暂未更新三类反馈。
- **Tests / gates**：后端覆盖 lifecycle、signature、双事务与 Active consumer；前端仍以 `pnpm check`、`pnpm build` 为提交门禁。
- **LLM 环境**：若当前环境缺少三项必需变量，只报告 Mock 与 mocked HTTP integration 验证，不伪造真实外部调用。

## 历史阶段：Phase 3-2 Real AI Tutor & Multi-Agent Orchestration

- **Provider**：保留 `BaseLLMProvider`；配置完整时使用 httpx `OpenAICompatibleProvider`，否则使用 `MockTutorProvider`。新 API 明确返回 provider 与 response_mode。
- **Agent Protocol**：集中在 `apps/api/app/agents/`，能力为 diagnosis / planning / tutoring / assessment。
- **Router**：显式 capability 优先；否则使用确定性关键词路由，未知默认 tutoring。
- **Agents**：Diagnosis/Planner/Tutor/Assessment 都包装既有 Service；不复制 mastery、diagnosis、planner 或 projection 算法。
- **Orchestrator**：`EducationAgentOrchestrator` 执行单能力或固定 Diagnosis → Planner → Tutor DAG；结构化 Agent 不调用 LLM，最终 Tutor 最多调用一次 Provider，不存在 Agent 循环。
- **Agent Trace**：只记录真实执行节点，前端显示实际协作顺序。
- **Assessment 安全边界**：读取最近真实 Practice Evidence，只解释结果，不写 Mastery；Mastery 仍只能由 `PracticeEvaluationService` 更新。
- **Planner 安全边界**：普通计划问题只读 Current Plan，只有明确生成/重新生成意图才写新 Plan。
- **Web**：`/#/xiaolian` 为“小涟 · 智能学习中枢”；全局与学习空间均调用 `POST /api/agents/chat`，旧 `/api/tutor/chat` 保持兼容。
- **Tests**：`uv run --project apps/api pytest apps/api/tests -q`：156 passed；`pnpm check` 与 `pnpm build` 已通过。
- **E2E**：真实 FastAPI + Vite + Chrome CDP 已验证协作 Trace、单 Tutor Trace、练习后 Assessment 与窄屏无横向溢出。
- **LLM 环境**：当前未配置 `EDUCATION_LLM_BASE_URL/API_KEY/MODEL`，因此真实外部 Provider 未请求；OpenAI-compatible 路径由 mocked HTTP integration tests 覆盖。

## 历史阶段：Phase 3-1 Intelligent Learning Workspace

- **首页 Dashboard**：真实 Profile、Diagnosis、Current Plan 与显式计划生成。
- **Latest / Current Plan**：保留唯一 ACTIVE 后端语义；比赛 Web 只展示当前计划，不提供 History 管理 UI。
- **Learning Workspace**：明确任务上下文、集中 Demo 教学内容、真实 learning_started 证据记录与安全无任务态。
- **Embedded Tutor**：学习空间和全局小涟共用 `POST /api/tutor/chat`，上下文由后端构建。
- **Practice Feedback**：`POST /api/practice/evaluate` 返回真实 mastery before/after，成功后等待 Profile 与 Diagnosis 刷新。
- **Learning Report**：复用 Profile、Diagnosis、Current Plan 与 Recent Evidence。
- **E2E**：FastAPI + Vite 验证首页、计划、任务、Tutor、Practice、刷新、返回首页与学习报告。
- **真实 / 静态边界**：学习者状态全部来自 API；只有课程讲解、固定题目和快捷问题为静态 Demo Content。
- **历史快照（Phase 3-1）**：当时尚未实现自动 Dynamic Replanning、Agent Tool Runtime、MCP Runtime 或课程检索；这些能力已分别在 Phase 3-2 至 3-5 补齐。当前仍未实现聊天历史记忆、知识图谱/向量数据库或 Digital Human。

## 历史阶段：第一阶段

第一阶段仅完成：独立项目初始化、教育 Web Workspace、FastAPI 最小骨架、领域模型基础类型、MCP 架构预留。后续章节保留历史演进记录。

**第一阶段明确不做**：真正多智能体、复杂数据库业务、真正 MCP Server Tool、知识图谱/RAG/向量库。

## 技术栈

- **Frontend**：React、TypeScript、Vite、React Router（hash）、Zustand、Tailwind CSS、Lucide React、Radix UI、React Hook Form、Zod、Axios
- **Backend**：Python、FastAPI、Pydantic、SQLAlchemy；依赖管理 `uv`
- **Database**：第一阶段 SQLite（迁移预留 PostgreSQL/MySQL，不写死 SQLite 特性）
- **AI**：统一 LLM Provider 抽象，支持未来 OpenAI-compatible / DeepSeek / Qwen

## 目录结构（三边界）

```text
{workspace}/                     # react-vite 前端（即 Web Workspace 本体）
├── src/
│   ├── domain/                  # TS 领域模型（Education Domain TS 版）
│   ├── mock/                    # 旧版展示数据（核心学习状态页面不使用）
│   ├── content/                 # 集中 Demo 教学内容（非 Learner State Mock）
│   ├── lib/                     # api 客户端 + 服务
│   ├── store/                   # Zustand 状态（小涟面板）
│   ├── components/              # 侧边栏 / 卡片 / 小涟面板等
│   └── pages/                   # 路由页面
├── apps/api/                    # FastAPI（Education API）
├── packages/                    # 跨语言共享领域（本轮以 TS src/domain + Python apps/api/domain 双实现）
├── mcp/server/                  # 真实 stdio MCP Server + shared Tool Registry
└── docs/                        # 架构文档
```

> 说明：当前工具链下共享 Python/TypeScript package 不便直接复用，故 **Education Domain** 采用双份实现：TS 版在 `src/domain`（供 Web 使用），Python 版在 `apps/api/domain`（供 API 使用），通过文档/README 保持定义一致，后续若引入 monorepo 工具链再收敛为共享包。

## 前端调用约定

- 路由 `createHashRouter`，`/#/` 为学习首页。
- 静态教学内容集中在 `src/content/`；核心学习状态页面不读取 `src/mock/dashboard.ts`。
- Profile、Diagnosis、Current Plan、Tutor Answer、Practice Evaluation 与 Recent Evidence 全部经真实 REST API 获取。
- 开发时 Vite 代理 `/api → http://localhost:8000`（FastAPI 后端，可用 `EDUCATION_API_URL` 覆盖）。

## SQL 操作 API / 工作流清单

本项目后端为**独立 FastAPI**（apps/api），非平台数据表工作流，因此**无端点 token**。
前端直接调用标准 REST 路由（同源，经 Vite 代理）：

| 功能 | 方法 | 路由 | 入参 | 出参 | 持久化 |
| --- | --- | --- | --- | --- | --- |
| 健康检查 | GET | `/api/health` | - | `{status, service}` | - |
| 开始学习 | POST | `/api/learning/start` | query: `learner_id`, `source`(current_study_plan/recommended_path), `knowledge_point_id?`, `course_id?`, `topic?` | `{message:"小涟已记录本次学习开始", evidence{id,evidence_type:learning_started,source,session_id,...}, session_id}` | 表 `learning_evidence` |
| 列证据 | GET | `/api/learning/evidence` | - | `LearningEvidenceOut[]` | 读表 |
| 练习评价 | POST | `/api/practice/evaluate` | body: `learner_id, course_id, knowledge_point_id, question_id, is_correct, score(0~1), difficulty(0~1)` | `{evidence, mastery_before, mastery_after, confidence, evidence_count, message}` | 表 `learning_evidence` + `mastery_records`（同事务） |
| 读掌握度 | GET | `/api/profile/mastery/{kp}` | query: `learner_id` | `{knowledge_point_id, mastery_score, confidence, evidence_count, updated_at}` | 读 `mastery_records` |
| 读学习画像 | GET | `/api/profile/{learner_id}` | query: `course_id`(默认 course-os) | `LearnerProfileOut`（Derived Read Model，动态聚合） | 读 `mastery_records` + `knowledge_points` |
| 读学习诊断 | GET | `/api/diagnosis/{learner_id}` | query: `course_id`(默认 course-os) | `DiagnosisResultOut`（primary_focus / priority_interventions / strengths / weak_points / developing_points / unassessed_points / summary_codes） | 动态计算（无持久化） |

> **Phase 2C（LearnerProfile → Diagnosis）**：`MasteryRecord`（Source of Truth）→ `LearnerProfileService`（Derived Read Model）→ `DiagnosisService` → `KnowledgePointDiagnosis[]`。
> 判定由 `KnowledgeDiagnosisPolicy`（集中阈值：MIN_EVIDENCE=1 / MIN_CONFIDENCE=0.2 / WEAK=0.50 / DEVELOPING=0.70 / PROFICIENT=0.85 / MASTERED=0.92）完成；`evidence_count=0` 判 `UNASSESSED` 绝不判 WEAK，证据不足判 `INSUFFICIENT_EVIDENCE`。
> **阈值作为各段独占上界**：`mastery < 0.50`→WEAK；`<0.70`→DEVELOPING；`<0.85`→PROFICIENT；`>=0.85`→MASTERED（`MASTERED_THRESHOLD=0.92` 为保留常量，非分支判定用）。判定顺序恒为 Evidence → Confidence → Mastery。
> `PriorityPolicy`：`priority = (1-mastery) × confidence`，钳制 `[0,1]`，仅 WEAK/DEVELOPING/PROFICIENT 可成为高优先级（UNASSESSED/INSUFFICIENT 不参与薄弱干预排名）。
> **第 2 步固化（LearnerProfile 聚合语义）**：`assessed` 仅 = WEAK/DEVELOPING/PROFICIENT/MASTERED（`KnowledgeDiagnosisPolicy.is_assessed`），`UNASSESSED` 与 `INSUFFICIENT_EVIDENCE` 均不计入 assessed；
> `coverage = assessed_count / total_knowledge_points`（不把未评估算作已评估）；
> `overall_mastery = Σ(mastery×confidence) / Σ(confidence)`，仅 assessed 参与，UNASSESSED/INSUFFICIENT 一律排除；`assessed_count==0` → `overall_mastery=null`（绝不返回 0.0/insufficient_data=true）；
> `overall_confidence = coverage × avg(assessed confidence)`，恒在 `[0,1]`，大量 UNASSESSED 会拉低整体可信度；
> `status_counts` 六状态之和 == `total_knowledge_points`；`unassessed_count = total - assessed`（含 UNASSESSED 与 INSUFFICIENT，二者可再经 `status_counts` 细分）。
> LearnerProfile 不含 `priority_score`（统一由 DiagnosisService 计算注入）。
> /`#/diagnosis` 已从占位升级为真实学习诊断页；/`#/` 首页「需要重点关注」接入真实 Diagnosis，
> 严格处理 loading/ready/error/empty（API 失败展示「暂时无法读取最新学习诊断 + 重新加载」，
> **不再降级 Mock 冒充真实诊断**；未评估知识点显示「尚未评估 / --」不显示 0%；
> 只消费 DiagnosisResult、不在前端重新诊断、不突出工程 priority 数值）。
> 学习证据闭环（已打通）：前端按钮 → `startLearning()`（`src/lib/educationApi.ts` + `src/store/useLearningStore.ts`）→
> Vite 代理 → `POST /api/learning/start` → `LearningEvidenceService` → `LearningEvidenceRepository` → SQLite →
> 返回 evidence id → 前端携带 plan/task/knowledge-point 上下文跳转 `/space`。
> **掌握度投影（Phase 2B）**：`learning_started` 是**行为证据**，不改变掌握度；
> `practice_answer_evaluated` 是**评价证据**，经由 `MasteryProjectionService` +
> `MasteryUpdatePolicy` 投影到 `mastery_records`（仅此类证据计入 assessment `evidence_count`）。
> Demo：`demo-user-001 + kp-pv` 初始 `mastery_score=0.58 / confidence=0.25 / evidence_count=1`（由 Seed 建立）。

其他占位路由（第一阶段）：`/api/profile`、`/api/diagnosis`、`/api/plans`、`/api/learning`、`/api/practice`、`/api/assessment`、`/api/reports`。

## Study Planner Domain（Phase 2D-0，仅领域基础，无持久化/无 API）

> **链路**：`DiagnosisResultOut` → `StudyPlannerService.generate_from_diagnosis(learner_id, course_id, diagnosis_result)` → `StudyPlanDraft`。
> Planner **只消费 DiagnosisResult**，绝不自行查询 Mastery 再重新诊断；不复制 Phase 2C 的 threshold/priority formula。

- **Planner 领域模型**（`apps/api/app/domain/planner.py`，纯 Pydantic Value Object，不落库）：
  - `PlannerActionType`：assess / remediate / strengthen / review。
  - `PlannerReasonCode`：PRIMARY_FOCUS / CONFIRMED_WEAKNESS / NEEDS_ASSESSMENT / NEEDS_MORE_EVIDENCE / NEEDS_STRENGTHENING / MAINTENANCE_REVIEW / NO_IMMEDIATE_INTERVENTION。
  - `PlanStrategy`：diagnosis_driven（第一版唯一策略；exam/goal/deadline_driven 未来再做）。
  - `StudyTaskDraft`：draft_key（`{kp_id}:{action}`，稳定 key）/ knowledge_point_id / knowledge_point_name / action_type / priority（**Planner 排序结果**，首个=1.0 递减，前端不得提交）/ estimated_minutes / reason_codes / source_status / source_priority_score / order。
  - `StudyPlanDraft`：learner_id / course_id / generated_at / strategy / tasks / reason_codes / source_diagnosis_generated_at；空计划 → `tasks=[]` + `reason_codes=[NO_IMMEDIATE_INTERVENTION]`；非空计划 reason_codes = 各任务原因去重汇总。
- **`StudyPlannerPolicy`**（`apps/api/app/services/study_planner_policy.py`，集中映射）：
  - 状态 → 动作：UNASSESSED/INSUFFICIENT_EVIDENCE → ASSESS（**未知 ≠ 薄弱**，绝不 REMEDIATE）；WEAK → REMEDIATE；DEVELOPING → STRENGTHEN；PROFICIENT → REVIEW；MASTERED → 不进入短期计划。
  - Action Tier（数值越小越优先）：REMEDIATE=1 < ASSESS=2 < STRENGTHEN=3 < REVIEW=4（理由：信息缺口先评估，比巩固更紧迫）。
  - `PlannerConfig`（集中配置）：`MAX_TASKS=3`；Duration：ASSESS=15 / REMEDIATE=35 / STRENGTHEN=25 / REVIEW=15（分钟）。禁止在 Service 散落 Magic Number。
- **`StudyPlannerService`**（`apps/api/app/services/study_planner_service.py`）：
  - 候选集 = DiagnosisResult 四类列表（weak_points / developing_points / strengths / unassessed_points）固定顺序拼接去重（互斥且完整覆盖）。
  - 排序 = (Action Tier, primary_focus 优先, -priority_score, 稳定序)；确定性：相同输入 → 相同 Plan。
  - primary_focus 存在且 eligible 时，必须成为最高合法补强任务（第一条 REMEDIATE/STRENGTHEN）。
- **当前 Demo Plan**（course-os，真实 Diagnosis）：① 死锁 kp-deadlock WEAK → REMEDIATE 35min（PRIMARY_FOCUS+CONFIRMED_WEAKNESS）→ ② 进程调度 kp-scheduling UNASSESSED → ASSESS 15min（NEEDS_ASSESSMENT）→ ③ 进程同步 kp-process-sync DEVELOPING → STRENGTHEN 25min（NEEDS_STRENGTHENING，priority 0.2 > PV 0.105）。

## StudyPlan Persistence（Phase 2D-1，仅后端持久化，无 API / 无 Web）

> **链路**：`DiagnosisResultOut` → `StudyPlannerService` → `StudyPlanDraft` → `StudyPlanPersistenceService.persist(draft)` → `StudyPlan` + `StudyTask[]` → SQLite（**同一事务**）。
> 形成第一条真实闭环：Diagnosis → Planner → Plan Draft → Persistent Study Plan。

- **持久化实体**（`apps/api/app/domain/models.py`）：
  - `StudyPlan`（表 `study_plans`）：`id`(UUID) / `learner_id` / `course_id` / `strategy`(diagnosis_driven) / `status`(active) / `generated_at` / `source_diagnosis_generated_at` / `reason_codes`(JSON) / `created_at` / `updated_at`。原占位字段（user_id/name/overall_progress/is_active）已移除。
  - `StudyTask`（表 `study_tasks`，本轮新建）：`id`(UUID，服务端生成) / `plan_id` / `knowledge_point_id` / `knowledge_point_name` / `action_type` / `priority`(**Planner 排序结果**) / `estimated_minutes` / `reason_codes`(JSON) / `source_status` / `source_priority_score`(**Diagnosis 输入原始分**) / `order` / `draft_key`(`{kp_id}:{action}`，**≠ DB primary key**) / `created_at` / `updated_at`。
  - `StudyPlanStatus`：active / superseded / completed；生成和自动重规划均通过 Lifecycle Service 在同一事务内完成 supersede → 新 Plan → Tasks。
- **Repository 层**（`app/services/study_plan_repository.py` / `study_task_repository.py`）：
  - `StudyPlanRepository`：`create(...)` / `get_by_id(...)` / `list_by_learner_and_course(...)`（Plan History，generated_at 倒序；不做 Active 唯一性）。
  - `StudyTaskRepository`：`create_many(...)` / `list_by_plan_id(...)`（**显式 ORDER BY order**）/ `to_domain(...)`（ORM → `PersistedStudyTask`）。
  - 事务纪律：Repository 只 add/flush/query，**不 commit**；Enum ↔ DB 表示转换在 Repository 层（保存稳定 enum value，JSON 由 SQLAlchemy JSON 列处理）。
- **`StudyPlanPersistenceService`**（`app/services/study_plan_persistence_service.py`）：`persist(draft) → PersistedStudyPlan`（plan + tasks 聚合输出模型，非 ORM）。Plan + N Tasks **同一业务事务**：全部成功 → commit；任何 Task 失败 → rollback Plan + 已建 Tasks（绝不半成功）。新 Plan 恒 ACTIVE；Empty Plan（tasks=[]，NO_IMMEDIATE_INTERVENTION）同样有效。
- **Provenance**：strategy / generated_at / source_diagnosis_generated_at / reason_codes / action_type / source_status / source_priority_score / order / draft_key 全部保存 —— 未来 Mastery / Diagnosis 变化后仍可解释旧计划为何这样生成。**不保存完整 Diagnosis JSON**。
- **输出模型**：`PersistedStudyPlan` / `PersistedStudyTask`（`app/domain/planner.py`，纯 Pydantic，供未来 HTTP / MCP / Agent Runtime 消费，ORM 不泄漏到上层）。
- **DB 建表**：无 Alembic，`Base.metadata.create_all` 建新表；`education.db` 旧 study_plans 占位 schema 由测试套件 drop_all+create_all 重建（`*.db` gitignored，dev DB 可重建）。未使用任何 runtime ALTER。
- **生命周期**：Phase 3-4 已通过 `StudyPlanLifecycleService` 收敛 Active Plan 读写；历史章节中的“未实现”描述仅保留为当时快照，不代表当前能力。

## StudyPlan Application / API（Phase 2D-2，仅后端 API，无 Web / 无 LLM / 无 MCP）

> **链路**：`POST /api/plans/generate` → `StudyPlanApplicationService.generate_plan` → `DiagnosisService`（真实诊断）→ `StudyPlannerService` → `StudyPlanDraft` → `StudyPlanPersistenceService.persist`（Plan + Tasks 同一事务）→ `PersistedStudyPlan` → HTTP Response。
> EducationMind 第一次具备三个真实应用能力：①显式生成诊断驱动 StudyPlan；②按 plan_id 读取完整 Plan + Tasks；③按 learner/course 查询 Plan History。

- **`StudyPlanApplicationService`**（`app/services/study_plan_application_service.py`）：
  - `generate_plan(learner_id, course_id)`：编排 Diagnosis → Planner → Persistence（复用，**不重新实现任何阈值/映射/排序/时长/MAX_TASKS/持久化映射**）；客户端只提交 learner/course，Diagnosis/Tasks 全部服务端确定性生成。课程名解析与 Diagnosis 路由一致（未知课程回退 course_id → 空诊断 → Empty Plan 201，不制造 404）。
  - `get_plan(plan_id)`：`StudyPlanRepository.get_by_id` + `StudyTaskRepository.list_by_plan_id`（ORDER BY order）重建完整 Plan；不存在返回 None → Route 404。**纯读取无副作用**。
  - `list_history(learner_id, course_id)`：`StudyPlanRepository.list_by_learner_and_course`（generated_at DESC，最新在前）+ `StudyTaskRepository.count_by_plan_id` → 摘要列表。Route 不二次排序。
  - 事务边界仍在 PersistenceService（ApplicationService 不 db.commit()）。
- **API**（`app/api/routes/plans.py`，复用原 plans.py Router，未创建第二套 plans 文件）：
  - `GET /api/plans?learner_id=&course_id=` → `PersistedStudyPlanSummary[]`（id/learner/course/strategy/status/generated_at/created_at/task_count/reason_codes，**不含 tasks**）。
  - `POST /api/plans/generate` → 201 `PersistedStudyPlan`（完整 Plan + Tasks）。
  - `GET /api/plans/{plan_id}` → `PersistedStudyPlan`；不存在 → 404 `{"detail":"study plan not found"}`。
  - 请求模型 `StudyPlanGenerateRequest`（`app/domain/models.py`）：learner_id/course_id `min_length=1` + strip 校验（空/空白 → 422）。
  - 输出模型复用 `PersistedStudyPlan`/`PersistedStudyTask`（已完美适配 API Response），新增 `PersistedStudyPlanSummary`（`app/domain/planner.py`）；**ORM 不泄漏到响应**（Route 不直接 SQL）。
- **Repository 增强**（ORM↔Domain 转换集中到 Repository 层）：`StudyPlanRepository.to_domain(plan, tasks)` / `to_summary(plan, task_count)`；`StudyTaskRepository.count_by_plan_id(plan_id)`；`StudyPlanPersistenceService._to_persisted` 委托 `StudyPlanRepository.to_domain`（消除两处转换实现）。
- **Active Plan 暂时语义（历史快照）**：Phase 2D-2 当时未实现唯一 ACTIVE；Phase 3-1 已改为 Lifecycle Service + Current Plan，Phase 3-4 又补齐动态重规划。以下保留用于说明演进，不代表当前约束。
- **日志**：generate 成功后轻量结构化日志（plan_id/learner_id/course_id/task_count），不记录 Diagnosis/Evidence 内容。
- **测试**：新增 19 项 API 测试（`tests/test_study_plan_api.py`，独立 tmp_path 临时 SQLite，覆盖 Test A~T：201 完整 Plan / 真实落库 study_plans+study_tasks / 字段正确 / order 排序 / detail / 404 / history 查询与 DESC / 生成后 +1 / 最新在前 / POST 与 GET 一致 / primary_focus 第一任务 / UNASSESSED→ASSESS 绝不 REMEDIATE / 空 learner|course 422 / Fake 证明不绕过 DiagnosisService / 持久化失败 500 且无半个 Plan / 连续两个 ACTIVE 不 supersede（**Phase 3-1 起改为 supersede 语义**）/ Empty Plan 201 可读回）。后端 `uv run pytest` **115 passed**；`pnpm check` / `pnpm build` 通过。
- **手动 E2E**（/tmp 临时 DB，未触碰 education.db）：history before=[] → generate×2 → 两个 ACTIVE Plan、detail 完整（orders [1,2,3]、draft_keys deadlock:remediate / scheduling:assess / process-sync:strengthen）、history after=2 且最新在前；404/422/未知课程 Empty Plan 201 均验证。
- 未实现（Phase 2D 后续，**Phase 3-1/3-4 已完成的除外**）：时间轴/倒排计划/Deadline Scheduler、LLM、MCP Tool、启动时自动生成。

## Current Plan Lifecycle（Phase 3-1，后端 + Web：Active 唯一性 + current API + Web 语义升级）

> **链路**：`POST /api/plans/generate` → `StudyPlanPersistenceService.persist`（**同一事务**：先 supersede 旧 ACTIVE → 建新 ACTIVE → commit）→ 任意时刻至多一个 ACTIVE 计划 → `GET /api/plans/current` 读取它。
> 正式取代 Phase 2D-2 的「latest plan ≈ current plan」临时 Web 语义。

- **Active 唯一性**（`study_plan_repository.py`）：`supersede_active_plans(learner_id, course_id, except_plan_id=None)`（UPDATE status→superseded，只 update 不 commit）+ `get_current(learner_id, course_id)`（status=active，ORDER BY generated_at DESC LIMIT 1 —— 排序仅兜底 Phase 3-1 之前遗留的多 ACTIVE 旧数据，不伪装唯一约束存在）。
- **事务原子性**（`study_plan_persistence_service.py`）：`persist(draft)` 在创建新 Plan **之前**调用 supersede，与 Plan + Tasks 共用**单一 commit 点** —— 任何一步失败 rollback 全部，绝不出现「旧计划已作废但新计划没生成」的空窗，也绝不出现两个 ACTIVE 并存。
- **API**（`plans.py`）：新增 `GET /api/plans/current?learner_id=&course_id=` → 200 `PersistedStudyPlan`（完整 Plan + Tasks）；无当前计划 → 404 `{"detail":"no current study plan"}`（GET 绝不自动生成）。⚠️ **路由顺序**：`/current` 注册在 `/{plan_id}` **之前**，避免被当作 plan_id。
- **Empty Plan 语义延续**：全部 MASTERED → Empty Plan（NO_IMMEDIATE_INTERVENTION）同样是合法 ACTIVE current，且会 supersede 旧计划（重新规划为空 → 无任务、不留双 ACTIVE）。
- **Web 语义升级**（`src/`）：`educationApi.fetchCurrentPlan`（404 → null 空状态，其余错误上抛）；`hooks.useCurrentPlan`（替代 `useLatestStudyPlan`：current 优先，404 → 无当前计划空态）+ `hooks.usePlanHistory`（历史列表）；`planApi.PLAN_STATUS_LABEL`（active→当前计划 / superseded→已更新 / completed→已完成）。消费方升级：`MyLearningPage`（主区块 = 当前计划 + 任务 Timeline + 「重新规划」按钮；新增**历史计划**列表，展示已 superseded 旧计划 latest 在前）、`TodayPlanCard`（首页，徽章改「当前计划」）、`LearningSpacePage`（「当前学习计划的任务」）、`ArchivePage`。
- **重新规划语义**：按钮仍是显式 `POST /plans/generate`；Practice 成功提交后还会触发确定性的 Dynamic Replanning，按 Material Change 决定是否 supersede 当前计划。两者都经由 Lifecycle Service 保证原子性。
- **测试**：新增 9 项（`tests/test_study_plan_current.py`，独立临时 SQLite，Test A~I：generate 后 current 返回该计划 / ×2 → 旧 superseded 新 active / 无计划 404 且不自动生成 / tasks order / 遗留多 ACTIVE 取 DESC 第一条 / Empty Plan supersede 旧计划 / persist 失败 500 且旧计划不被 supersede（Fake 原子性）/ /current 不被 /{plan_id} 遮蔽（404 detail 可区分）/ 不同 learner 作用域隔离）。既有 Test S 由「两个 ACTIVE 并存」改为「supersede 语义」。后端 `uv run pytest` **135 passed**；`pnpm check` / `pnpm build` 通过。
- 明确未实现（Phase 3-1 边界，已由 Phase 3-4 补齐动态证据触发）：无时间轴 / deadline 驱动的定时重规划、历史计划详情展开、真实 LLM、MCP Tool。

## Tutor Agent（Phase 3-0，后端 + Web，LLM Provider 抽象 + Mock）

> **链路**：`POST /api/tutor/chat` → `TutorService.chat` → `TutorContextBuilder.build`（Profile / Diagnosis / StudyPlan / Evidence **全部复用既有服务**）→ `TutorPromptBuilder.build_messages`（集中 Prompt）→ `llm_provider.chat(messages, context=...)`（Provider 抽象）→ `TutorResponse`（answer / context_used / suggested_actions / source）。
> EducationMind 第一次具备「学习上下文驱动 AI Tutor」：学生提问 → 读取真实学习上下文 → 生成个性化回答（而非普通 ChatGPT）。

- **领域模型**（`app/domain/tutor.py`，纯 Pydantic，不落库）：
  - `TutorConversationRequest`：learner_id / course_id / message（min_length=1 + strip → 422）。
  - `TutorResponse`：answer / context_used（**解释能力**：本次回答实际用了哪些学习上下文，如 `["profile","diagnosis","study_plan"]`，不是返回全量数据）/ suggested_actions / source（`llm` | `fallback`）。
  - `TutorContext` 及子模型：profile / diagnosis / plan / recent_evidence —— **请求级快照，不保存聊天历史、不建 Conversation 表**。
- **`TutorContextBuilder`**（`app/services/tutor_context_builder.py`，本轮核心）：
  - Profile → `LearnerProfileService.build_profile`（**只构建一次**）；Diagnosis → `DiagnosisService.build_from_profile`（复用同一 Profile）。
  - Plan → `StudyPlanRepository.list_by_learner_and_course` 取 `generated_at DESC` 第一条 + `StudyTaskRepository.list_by_plan_id`（**无 current plan lifecycle / Active 唯一语义**）。
  - Evidence → `LearningEvidenceRepository.list_recent_by_learner`（新增最小方法，最近 5 条）。
  - **禁止重复计算**：不自己算 mastery / 不自己判 weak / 不自己生成 priority；保持 Mastery → Diagnosis → Tutor 单向链路。
- **集中 Prompt**（`app/services/tutor_prompt.py`）：`SYSTEM_PROMPT` 集中常量 + 上下文确定性渲染，字符串不散落。
- **LLM Provider**（`app/llm/`）：复用既有 `BaseLLMProvider` 抽象（**未创建 OpenAIClient / ClaudeClient / LLMClientV2**）；`get_llm_provider()` 当前返回 `MockTutorProvider`（接口真实、确定性、上下文感知，通过 `chat(messages, context=...)` 接收 TutorContext）；未绑定 OpenAI。
- **Fallback（比赛演示稳定优先）**：LLM 失败 → 确定性兜底回答 + `source="fallback"` **诚实标记**，绝不伪装成 LLM 输出。
- **API**（`app/api/routes/tutor.py`，已注册）：`POST /api/tutor/chat` → `TutorResponse`；Route 不直接调 LLM（保持 Application Layer）。
- **Web**（`src/pages/XiaolianPage.tsx`，路由 `/#/xiaolian`）：小涟聊天窗口 —— 顶部「小涟 · AI学习导师」/ 中间聊天区（用户右、小涟左）/ 底部输入框 + 发送按钮；loading 显示「小涟正在分析你的学习情况...」、error 显示「小涟暂时无法连接，请稍后再试。」；展示 context_used 中文徽章（已参考：学习画像/诊断/计划/最近记录）与 suggested_actions；三个演示问题快捷按钮。Demo 用户 demo-user-001 + course-os（常量与 Seed 一致）。
- **测试**：新增 11 项（`tests/test_tutor.py`，Test A~I：ContextBuilder 读 Profile / Diagnosis / StudyPlan / 无 Plan 正常 / TutorService 调用 LLM Provider / LLM 失败 fallback / API 返回结构 / 空 message 422 / Demo 死锁含 diagnosis context）。后端 `uv run pytest` **126 passed**；`pnpm check` / `pnpm build` 通过。
- **明确未实现**（不夸大）：RAG / MCP / Multi-Agent / Memory System（无聊天历史持久化）/ 数字人 / TTS / 知识图谱 / 真实 LLM API Key（当前 Mock Provider 接口真实）。未来接真实模型：只改 `get_llm_provider()`。

## 变更记录

- 2026-08-25（Phase 3-6 Live2D + Learning Workshop）：
  - 本地安装 Cyrene1002 与 Cubism Core，加入安全 ZIP 安装器、开发专用资源挂载和 Git/构建隔离；实现 Pixi/Cubism4 懒加载、单实例 WebGL 租约、Ticker、响应式构图、指针视线与讲解口型。
  - 新增 Wikipedia 联网检索、`c-edu` 教学编译模拟、五类课程模板资源生成及 `/#/resources` 学习工坊；课程内 RAG、外部资料、学习证据三者边界保持分离。
  - 新增浏览器 TTS 播放/停止并接入小涟页和学习空间；补充新路由、六项学习星轨、加载回退与站点图标。
  - 最终质量门禁：后端 232 tests；前端测试、`pnpm check`、生产构建、三项真实 API smoke、桌面/移动端浏览器流程与受限资产扫描均需同时通过后交付。
- 2026-08-18（Phase 3-1 Current Plan Lifecycle）：
  - **Active 唯一性**：`StudyPlanRepository.supersede_active_plans` + `get_current`；`StudyPlanPersistenceService.persist` 在创建新 Plan 前于**同一事务**内 supersede 旧 ACTIVE（单一 commit 点，失败全回滚）；`StudyPlanApplicationService.get_current`；`GET /api/plans/current`（404 = 无当前计划，注册在 /{plan_id} 之前避免路由遮蔽）。
  - **Web 语义升级**：`fetchCurrentPlan`（404 → null）/ `useCurrentPlan`（替代 useLatestStudyPlan）/ `usePlanHistory` / `PLAN_STATUS_LABEL`；`MyLearningPage`（当前计划 + 「重新规划」 + 历史计划列表）、`TodayPlanCard`（徽章改「当前计划」）、`LearningSpacePage`、`ArchivePage` 全部升级为 current 语义；「重新规划」= 显式 POST /plans/generate（自动 supersede 旧计划），非自动重规划。
  - **测试**：新增 9 项 `tests/test_study_plan_current.py`（Test A~I，含 supersede 事务原子性 Fake、路由遮蔽、多 ACTIVE 遗留兜底、Empty Plan supersede、learner 作用域隔离）；既有 Test S 改为 supersede 语义。后端 `uv run pytest` **135 passed**；`pnpm check` / `pnpm build` 通过。
  - 明确未实现（当时快照）：自动/定时 Dynamic Replanning、历史计划详情展开页、真实 LLM、MCP Tool。Dynamic Replanning 已由 Phase 3-4 实现；本项目仍不做时间轴 / deadline scheduler。
- 2026-08-16（Web 表现层完善，4a0c885）：学习首页完整化（HeroBanner / StatCards / CapabilitiesCard / HomeProfileCard / HomeDiagnosisCard / TodayPlanCard / WeakPointsCard / LearningPathCard / PlanOverviewCard / TodayTasksCard）；`/#/my-learning` 从占位升级为真实计划页（最新计划 + 任务 + 开始学习）；`/#/space` 完整学习空间（当前任务状态条 / 学习内容 Module / 快速练习 ModulePractice / 右侧小涟 SpaceTutor）；`/#/archive` 学习报告页（Profile + Diagnosis + Latest Plan + Recent Evidence）；`educationApi` 补齐 Plan/Tutor/Evidence 客户端 + `hooks.ts` 统一 API Hooks（useLearnerProfile/useDiagnosis/useLatestStudyPlan/useTutorChat/useRecentEvidence）+ `useWorkspaceStore`（plan/task/kp 上下文传递）+ `domain/planApi.ts`（PersistedStudyPlan 契约）。该批 Web 成果当时未登记 .project.md，本次补登。
- 2026-08-15（Phase 3-0 Tutor Agent Foundation）：
  - 建立 Tutor Agent Layer：`app/domain/tutor.py`（TutorConversationRequest / TutorResponse / TutorContext 请求级快照）+ `TutorService`（Application Layer，Route 不直接调 LLM）+ `TutorContextBuilder`（复用 LearnerProfileService / DiagnosisService / StudyPlanRepository / LearningEvidenceRepository，禁止重复计算）+ `TutorPromptBuilder`（集中 Prompt）。
  - LLM：复用既有 `BaseLLMProvider` 抽象；`get_llm_provider()` 默认 `MockTutorProvider`（接口真实、确定性、上下文感知）；LLM 失败 → 确定性 fallback（`source="fallback"` 诚实标记）。
  - API：`POST /api/tutor/chat`（Request learner_id/course_id/message，空/空白 → 422；Response answer/context_used/suggested_actions/source）。
  - Web：`/#/xiaolian` 小涟聊天窗口（`src/pages/XiaolianPage.tsx` + `educationApi.chatWithTutor`），替换原占位页。
  - Demo 三问浏览器全验证（真实 education.db）：死锁→Diagnosis（46% 薄弱）、今天学什么→StudyPlan（3 任务：死锁补弱 35min / 进程调度评估 15min / 进程同步巩固 25min）、PV→LearnerProfile（58% 发展中）；已为 demo-user-001 生成真实 StudyPlan 使「今天学什么」可引用计划。
  - 测试：新增 11 项 Tutor 测试（tests/test_tutor.py）。后端 `uv run pytest` 126 passed；`pnpm check` / `pnpm build` 通过。
  - 明确未实现：RAG / MCP / Multi-Agent / Memory（无聊天历史表）/ 数字人 / TTS / 真实 LLM Key。
- 2026-08-12：EducationMind 第一阶段初始化；建立 TS 领域模型、集中 Mock、教育 Web Workspace、FastAPI 骨架、MCP 预留、README。
- 2026-08-12：打通「学习开始」真实证据链路 —— 首页「继续学习 / 开始这段学习」通过 `POST /api/learning/start` 写入 LearningEvidence 到 SQLite 并跳转学习空间；新增 LearningEvidenceService/Repository、Zustand store、Vite `/api` 代理。
- 2026-08-12（Phase 2B）：建立 LearningEvidence → MasteryRecord 投影链路 —— 收敛 `evidence_type`/`source` 语义（learning_started/practice_answer_evaluated + current_study_plan/recommended_path/learning_space）、新增 `MasteryRepository`/`MasteryProjectionService`/`MasteryUpdatePolicy`/`PracticeEvaluationService`（事务边界）、`POST /api/practice/evaluate`、`GET /api/profile/mastery/{kp}`、Demo Seed；前端 `/space` 增加真实练习「PV 操作 · 快速练习」，首页 PV 掌握度读取真实值；统一 Demo 用户 `demo-user-001`。
- 2026-08-12（Phase 2C）：建立 MasteryRecord + LearningEvidence → LearnerProfile → Diagnosis 真实诊断链路 —— 新增 `DiagnosisStatus`/`DiagnosisReasonCode`/`KnowledgePointDiagnosis` 领域模型、`DiagnosisThresholds`/`KnowledgeDiagnosisPolicy`/`PriorityPolicy`（集中阈值，UNASSESSED 与 WEAK 严格区分）、`LearnerProfileService`（Derived Read Model）、`DiagnosisService`（结构化诊断，无 LLM）、`KnowledgePointRepository`、Demo 知识点与掌握基线（course-os 五知识点）；新增 `GET /api/profile/{learner_id}`、`GET /api/diagnosis/{learner_id}`；前端 `src/domain/diagnosis.ts` 类型 + `educationApi` 客户端 + `/#/diagnosis` 真实页 + 首页「需要重点关注」接入真实诊断；修复旧 `LearnerProfile`/`MasteryRecord` TS 占位类型导致的 tsc 报错；后端 30 项测试全部通过。
- 2026-08-12（Step 1 Diagnosis Domain 固化审查）：复核 Diagnosis Domain 全链路正确性；确认阈值集中、状态互斥、UNASSESSED 绝不为 WEAK、priority 仅合格知识点参与。补充测试：状态阈值边界（0.50/0.70/0.85/0.92 处转换无歧义 + 浮点越界防御）、priority 恒在 `[0,1]`（含全状态聚合校验）。后端 37 项测试全部通过。
- 2026-08-12（Step 2 LearnerProfile/Diagnosis Application 层固化审查）：复核 LearnerProfileService / DiagnosisService / Profile & Diagnosis API 全链路正确性，**无需改后端代码**（实现已满足：LearnerProfile 为 Derived Read Model 动态聚合、MasteryRecord 为唯一事实源、UNASSESSED 也出现在 Profile 并判为未评估、assessed 仅 WEAK/DEVELOPING/PROFICIENT/MASTERED）。补充 13 项聚合语义测试，覆盖：coverage 不计 UNASSESSED/INSUFFICIENT、overall_mastery 排除 UNASSESSED+INSUFFICIENT、无 assessed → overall_mastery=null、confidence 加权、status_counts 总和=total、primary_focus=最高合法 priority（UNASSESSED/INSUFFICIENT 绝不成为焦点、全部 MASTERED → null）、Practice 后 Profile/Diagnosis 动态刷新、overall_confidence 恒在 `[0,1]` 且大量 UNASSESSED 拉低可信度。后端 50 项测试全部通过；`pnpm check` / `pnpm build` 通过；手动 API 与动态刷新验证通过（overall_mastery 0.639 / overall_confidence 0.366 / coverage 0.8 / primary_focus=kp-deadlock(weak)）。
- 2026-08-12（Step 3 Diagnosis Web 表现层固化审查）：复核 `/#/diagnosis` 全链路（GET LearnerProfile + GET Diagnosis → educationApi → DiagnosisPage），确认已完全由真实 API 驱动、无 Mock 回退。原实现足够，仅两处补强于 `src/pages/DiagnosisPage.tsx`：①知识点诊断列表补显示「有效评价证据 N 条」；②重点关注的工程化 priority 数值弱化为「优先关注」标签。浏览器验证全部通过：真实请求 GET /api/profile|diagnosis/demo-user-001?course_id=course-os；UNASSESSED（进程调度）显示「尚未评估」+「--」不显示 0%/薄弱；primary_focus（死锁）动态展示；API 失败显示「暂时无法生成学习诊断，请稍后再试。」不回落 Mock；overall_mastery=null（空 learner）显示「暂无足够数据」非 0%/NaN%。后端 `uv run pytest` 50 passed；`pnpm check` / `pnpm build` 通过。修改范围仅 DiagnosisPage + .project.md。
- 2026-08-13（Phase 2C Final / Step 4 首页真实 Diagnosis + E2E 闭环封板）：
  - **修复**：`POST /api/learning/start` 路由原先不 commit，导致 `learning_started` 仅返回对象却不落盘。已在 `apps/api/app/api/routes/learning.py` 将 `learning/start` 作为该写操作业务事务边界统一 `db.commit()`（与 `practice/evaluate` 单事务一致）。新增后端回归测试 `test_learning_started_evidence_is_persisted`（后端 51 passed）。
  - **首页**：`src/components/home/WeakPointsCard.tsx` 重写为严格状态机（loading/ready/error/empty）。①移除 error 态 Mock 薄弱点回退，改为「暂时无法读取最新学习诊断 + 重新加载」；②ready 态展示真实 `priority_interventions` Top 3（沿用后端 `priority_score DESC`，前端不重排）；③小涟建议由结构化 `primary_focus` 确定性生成（不调用 LLM）；④未评估知识点显示「尚未评估 / --」不显示 0%；⑤只消费 DiagnosisResult、不在前端重新诊断；⑥移除 `useMasteryState('kp-pv', 0.58)` Mock 取值依赖。
  - **E2E（临时库 `/tmp/opencode/e2e_education.db`，未触碰真实 Demo 库）**：learning_started 落盘且 kp-pv mastery 不变（0.58）；正确练习 mastery 0.58→0.635；错误练习 0.635→0.5788；`/#/diagnosis` 与首页均读最新 recompute；primary_focus 保持 kp-deadlock（正常）。
  - **Demo 数据**：验证真实 `apps/api/education.db` 未被 E2E 触碰，kp-pv 仍为 Seed 基线 0.58/0.25/1。
  - **文档**：README.md 核心闭环更新为 Learning Action → LearningEvidence → MasteryProjection → MasteryRecord → LearnerProfile → Diagnosis → Education Web；新增「未实现」清单（Planner/AI Tutor/知识图谱/RAG/MCP Runtime 等不提前宣传）+ 算法传闻真实性声明 + UNKNOWN≠WEAK 可信性原则；`docs/architecture.md` 核心闭环同步。
  - 测试：后端 `uv run pytest` 51 passed；`pnpm check` / `pnpm build` 通过。
- 2026-08-14（Phase 2D-0 Study Planner Domain Foundation）：
  - 建立 Planner 领域基础（**无持久化、无 API、无前端、无 LLM、无 MCP**）：新增 `app/domain/planner.py`（PlannerActionType / PlannerReasonCode / PlanStrategy / StudyTaskDraft / StudyPlanDraft，纯 Pydantic Value Object）、`app/services/study_planner_policy.py`（PlannerConfig：MAX_TASKS=3 + Duration 集中；状态→动作→Tier→Reason 集中映射，UNASSESSED/INSUFFICIENT→ASSESS 绝不 REMEDIATE，MASTERED 不进入短期计划）、`app/services/study_planner_service.py`（`generate_from_diagnosis`：消费 DiagnosisResultOut，Tier + primary_focus + priority_score + 稳定序排序，MAX_TASKS 截断，空计划 NO_IMMEDIATE_INTERVENTION）。不改 Phase 2C 任何实现（KnowledgeDiagnosisPolicy / PriorityPolicy / MasteryUpdatePolicy / LearnerProfileService / DiagnosisService 零改动）。新增 30 项 Planner 测试（Test A~O + 分层/确定性/空计划/时长/原因码/优先级语义）。后端 `uv run pytest` 81 passed；`pnpm check` / `pnpm build` 通过。Demo Plan：死锁 REMEDIATE → 进程调度 ASSESS → 进程同步 STRENGTHEN。
- 2026-08-14（Phase 2D-1 StudyPlan Persistence Foundation）：
  - **演进 `StudyPlan` ORM**（移除占位字段 user_id/name/overall_progress/is_active，改为真实业务字段：learner_id / course_id / strategy / status / generated_at / source_diagnosis_generated_at / reason_codes），**新增 `StudyTask` ORM**（study_tasks 表，含完整 provenance）与 `StudyPlanStatus`（active/superseded/completed，本轮只建 ACTIVE）。**未创建第二套 Plan 表**（不叫 StudyPlanV2 等）。
  - **新增** `app/services/study_plan_repository.py`、`study_task_repository.py`（Repository 只 add/flush/query 不 commit；list_by_plan_id 显式 ORDER BY order；Enum↔DB 转换集中在 Repository）、`app/services/study_plan_persistence_service.py`（`persist(draft)`：Plan + N Tasks **同一事务**，失败全回滚，返回 `PersistedStudyPlan` 聚合输出模型）。
  - **Planner Provenance**：strategy / source_diagnosis_generated_at / reason_codes / action_type / source_status / source_priority_score / priority / order / draft_key 全部持久化（draft_key ≠ DB id；priority 与 source_priority_score 两个字段严格分离）。**不保存完整 Diagnosis JSON**。
  - **Empty Plan 支持**：tasks=[] + NO_IMMEDIATE_INTERVENTION 可正常保存为 ACTIVE 计划。
  - **未做**：Planner API、Planner Web、Dynamic Replanning、Active Plan 唯一性、LLM、MCP、Seed 假计划、启动时自动生成、runtime ALTER、Alembic。
  - **测试**：新增 15 项 Persistence 测试（tests/test_study_plan_persistence.py，独立临时 SQLite :memory:，覆盖 Test A~O：persist 成功/字段完整/读取重建/顺序稳定/Empty Plan/事务回滚/Repo 不 commit/服务端 ID/draft_key≠id/priority 与 source_priority_score 分离/三类 Enum round-trip/真实 Demo 集成）。后端 `uv run pytest` 96 passed；`pnpm check` / `pnpm build` 通过。手动验证（/tmp 临时 DB）：Plan active + diagnosis_driven + 3 tasks，与 Phase 2D-0 Demo Draft 完全一致。
- 2026-08-14（Phase 2D-2 StudyPlan Application Service & API）：
  - **新增** `app/services/study_plan_application_service.py`（generate_plan / get_plan / list_history 编排，复用 DiagnosisService → StudyPlannerService → StudyPlanPersistenceService，客户端不可提交 Diagnosis/Tasks，事务边界保持在 PersistenceService）。
  - **升级** `app/api/routes/plans.py`（复用原文件）：`GET /api/plans`（History 摘要）、`POST /api/plans/generate`（201）、`GET /api/plans/{plan_id}`（404 语义）；移除占位 `current/{user_id}` Stub（Current Plan 属后续阶段）。
  - **新增** `StudyPlanGenerateRequest`（models.py，min_length+strip → 422）、`PersistedStudyPlanSummary`（planner.py）；Repository 层新增 `to_domain`/`to_summary`/`count_by_plan_id`，转换逻辑集中 Repository（PersistenceService 委托）。
  - **测试**：新增 19 项 API 测试（tests/test_study_plan_api.py，临时 tmp_path SQLite，Test A~T）。后端 `uv run pytest` **115 passed**；`pnpm check` / `pnpm build` 通过。手动 E2E（/tmp 临时 DB）验证生成/详情/历史/404/422/Empty Plan/连续双 ACTIVE。
  - **明确未做**：Current Plan API、Active 唯一性、自动 supersede、Dynamic Replanning、Planner Web（src/ 零改动）、LLM、MCP Tool、启动时自动生成。
